String.prototype.toJadenCase = function (input) {
    const inputArray = input.split(" ");
    let result = [];
    inputArray.forEach((curr) => {
        curr[0]
    })
};

console.log(String.prototype.toJadenCase("How can mirrors be real if our eyes aren't real"));